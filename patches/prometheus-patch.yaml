apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: otel-demo
spec:
  template:
    spec:
      containers:
      - name: prometheus-server
        resources:
          requests:
            memory: "512Mi"
          limits:
            memory: "1Gi"
